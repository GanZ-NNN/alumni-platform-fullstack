name: backend
description: A server app using the shelf package and Docker.
version: 1.0.0

environment:
  sdk: ^3.10.7

dependencies:
  dbcrypt: ^2.0.0
  dotenv: ^4.2.0
  path: ^1.9.1
  postgres: ^3.5.9
  shelf: ^1.4.2
  shelf_router: ^1.1.2
  shelf_static: ^1.1.3
  dart_jsonwebtoken: ^2.7.1

dev_dependencies:
  http: ^1.2.2
  lints: ^6.0.0
  test: ^1.25.6
