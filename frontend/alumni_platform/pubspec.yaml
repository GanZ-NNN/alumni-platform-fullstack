name: alumni_platform
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.6.0
  flutter_secure_storage: ^9.2.4
  image_picker: ^0.8.7+4
  http_parser: ^4.0.0
  intl: ^0.20.2
  fl_chart: ^0.68.0
  url_launcher: ^6.3.0 # Added as required

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
